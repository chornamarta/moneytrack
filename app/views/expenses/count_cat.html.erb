<h3 class="text-center mt-5" id="text-zag">Statistic by category</h3>

<div class="container  mt-5">
  <div class="row justify-content-center">
    <div class="col-4">
      <ul class="list-group">
        <% @expenses.each do |k,v| %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <%=k%>
            <span class="badge  badge-info badge-pill"> <%=v%>$</span>
          </li>
        <%end%>
      </ul>
   </div>
  </div>
</div>